# This YAML file defines a GitHub Actions workflow named "Run Tests" that runs tests for a Laravel API project.
name: Run Tests

on:
  push:
    branches:
      - main
  pull_request:
    types: [opened, synchronize, reopened]

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:

      - name: Checkout Code
        uses: actions/checkout@v4
      
      - name: Setup PHP Action
        uses: shivammathur/setup-php@2.31.1
        with:
          php-version: '8.3'
          extensions: pdo, pdo_pgsql, zip
          tools: composer:v2, php-cs-fixer, phpunit

      - name: Install Dependencies
        run: composer install

      - name: Prepare Laravel Application
        run: |
          cp .env.example .env
          php artisan key:generate

      - name: Run Tests
        run: |
          echo "DB_CONNECTION=pgsql" >> .env
          echo "DB_URL=postgresql://postgres.xadtqkvxwpbuauxeoell:wow01777121518@aws-0-sa-east-1.pooler.supabase.com:6543/postgres" >> .env
          echo "DB_HOST=aws-0-sa-east-1.pooler.supabase.com" >> .env
          echo "DB_PORT=6543" >> .env
          echo "DB_DATABASE=postgres" >> .env
          echo "DB_USERNAME=postgres.xadtqkvxwpbuauxeoell" >> .env
          echo "DB_PASSWORD=wow01777121518" >> .env
          php artisan test